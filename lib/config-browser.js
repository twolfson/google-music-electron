// Load in our dependencies
// TODO: Load in `configstore`
// var ipc = require('ipc');

// When the DOM loads
window.addEventListener('DOMContentLoaded', function handleDOMLoad () {
  // Fill out existing values

  // Add change bindings
  console.log('sup');
});
